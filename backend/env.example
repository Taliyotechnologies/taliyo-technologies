# Database Configuration
MONGO_URI=mongodb+srv://taliyo_user:Taliyo019@cluster0.kmbp5ro.mongodb.net/taliyo?retryWrites=true&w=majority&appName=Cluster0
# For local development:
# MONGO_URI=mongodb://localhost:27017/taliyo

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Credentials (for initial login)
ADMIN_EMAIL=admin@taliyotechnologies.com
ADMIN_PASSWORD=changeme

# Email Configuration (Gmail)
EMAIL_USER=your_gmail@gmail.com
EMAIL_PASS=your_gmail_app_password

# Frontend URL (for CORS and email links)
FRONTEND_URL=https://taliyotechnologies.com
# For local development:
# FRONTEND_URL=http://localhost:5173

# Web Push VAPID Keys (generate using web-push or keep empty for ephemeral dev keys)
# VAPID_PUBLIC_KEY=YOUR_PUBLIC_KEY
# VAPID_PRIVATE_KEY=YOUR_PRIVATE_KEY

# Environment
NODE_ENV=production
# For local development:
# NODE_ENV=development

# Port (Railway will set this automatically)
PORT=5000 